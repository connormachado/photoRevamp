One time if you want a hard reset on the database:
    rm -rf ./photo_db

New run commands

  # Backend (from repo root)
  source .venv/bin/activate
  python3 backend/server.py            # port 5001, --db defaults to ./photo_db

  # Re-indexing later (unchanged behavior)
  python3 backend/embed_photos.py --photos ~/Pictures/"Photos Library.photoslibrary"/originals/

  # Frontend
  cd photo-search && npm run dev      # Vite on :5173